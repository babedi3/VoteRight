import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding:10,
    },
    image: {
        height: 50,
        width: 50,
        borderRadius: 40,
    },
    text: {
        padding:15,
    },
});

export default styles;