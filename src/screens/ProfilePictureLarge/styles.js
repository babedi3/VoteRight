import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding:10,
    },
    image: {
        
        height: 200,
        width: 200,
        borderRadius: 40,
        padding:15,
        
    },
});

export default styles;